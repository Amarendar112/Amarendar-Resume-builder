services:
  # Vite Frontend Static Site (100% Free, No Credit Card Required)
  - type: web
    name: amarendar-resume-builder
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      # SPA routing fallback
      - type: rewrite
        source: /*
        destination: /index.html
